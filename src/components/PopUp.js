export default function Popup ({ message }) {
    return (
        <div>
            <p>{message}</p>
            <button>Yes</button>
            <button>No</button>
            <button>Okay</button>
        </div>
    )
}